(function() {
    'use strict';

    angular
      .module('horizon.dashboard.container.bigdataClusters')
      .controller('horizon.dashboard.container.bigdataClusters.DrawerController', controller);
  
    controller.$inject = [
    ];
  
    function controller() {
    }
  
  })();